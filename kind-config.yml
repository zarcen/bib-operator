kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  # extraMounts:
  #   - hostPath: ./build-output
  #     containerPath: /builds
  #   # Comment out the following line if you want to use the default registry.
  #   - hostPath: /var/run/docker.sock
  #     containerPath: /var/run/docker.sock